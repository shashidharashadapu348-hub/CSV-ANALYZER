services:
  - type: web
    name: csv-analyzer
    runtime: static
    buildCommand: bun install && bun run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
