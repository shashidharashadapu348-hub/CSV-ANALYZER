/** Max rows loaded from a CSV to keep the UI responsive in the browser. */
export const MAX_CSV_ROWS = 2000;

/** Max rows sampled for NLP / embedding visualizations (t-SNE is O(n²)). */
export const MAX_NLP_SAMPLE = 200;
